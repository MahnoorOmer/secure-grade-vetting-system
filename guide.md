# Running the project

To run the backend code, follow these steps:

1. Open a terminal or command prompt.
2. Navigate to the backend folder in your project directory (e.g., `cd backend`).
3. Ensure dependencies are installed by running `npm install` if not already done.
4. Start the development server with the following command:

    ```bash
    npm run dev
    ```

